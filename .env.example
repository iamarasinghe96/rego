# Credentials for the edge middleware gate. Set these in Vercel under
# Settings → Environment Variables (Production, Preview and Development).
#
# Do NOT commit real values — this file is a template only. If both are
# unset the middleware denies every request rather than failing open.
SITE_USER=changeme
SITE_PASSWORD=use-a-long-random-passphrase
